use godot::prelude::godot_print;
use crate::player::Player;

pub trait Interactable {

    /*
    fn interact(/*this : impl Interactable*/ &mut self , player : &mut Player) {
        godot_print!("interacted with")
    }
     */

}